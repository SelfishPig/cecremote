[project]
name = "cec-remote"
version = "0.1.0"
description = "REST API for controlling HDMI-CEC through a persistent cec-client"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115,<1",
    "uvicorn[standard]>=0.30,<1",
]

[tool.uv]
package = false
